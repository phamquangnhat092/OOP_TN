- Lab01:
None
- Lab02:
+ When should accessor methods be used?: Khi chúng ta cần truy cập đến các thuộc tính của lớp từ bên ngoài
+ If you create a constructor method to build a DVD by title then create a constructor method to build a DVD by category. Does JAVA allow you to do this?: Có JAVA có hỗ trợ, và đó gọi là Nạp chồng phương thức
- Lab03:
+ Is JAVA a Pass by Value or a Pass by Reference programming language?: JAVA is a Pass by Value programming language.
- Lab04:
+ What class should implement the Comparable interface?: MediaComparatorByCostTitle và MediaComparatorByTitleCost.
+ In those classes, how should you implement the compareTo()method be to reflect the ordering that we want?: với MediaComparatorByCostTitle thì sẽ so sánh giá rồi so sánh tiêu đề còn MediaComparatorByTitleCost thì ngược lại so sánh tiêu đề trước rồi mới so sánh giá.
+ Can we have two ordering rules of the item (by title then cost and by cost then title) if we use this Comparable interface approach?: Có thể sử dụng như vậy.
+ Suppose the DVDs has a different ordering rule from the other media types, that is by title, then decreasing length, then cost. How would you modify your code to allow this?: Ta có thể sử dụng instanceof để kiểm tra xem media này có phải là DVD không rồi sau đó xét điều kiện sắp xếp sau, chỉ là thêm phần if else.

- Lab05:
None